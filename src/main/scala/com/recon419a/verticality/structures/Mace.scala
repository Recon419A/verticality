package com.recon419a.verticality.structures

import com.recon419a.verticality.util.{Coordinate, Size}

object Mace {
  def apply(roomSize: Size, hallwaySize: Size): Unit = {
//    Room(roomSize) + (Hallway(hallwaySize))
  }
}
